# คู่มือการ Deploy NzBill ไปยัง Vercel 🚀

การ Deploy หรือนำเว็บไซต์ขึ้นใช้งานจริงบน Vercel นั้นทำได้ง่ายมากครับ ทำตามขั้นตอนด้านล่างได้เลย

## สิ่งที่ต้องเตรียม
1. **GitHub Account** หรือ **Email** สำหรับ Login Vercel
2. **Internet** ที่เสถียร

---

## วิธี Deploy (แบบง่ายที่สุด)

เนื่องจากเราตั้งค่าโปรเจคไว้เรียบร้อยแล้ว ครั้งต่อไปที่คุณแก้โค้ดเสร็จแล้วอยากเอาขึ้น Web จริง ให้ทำดังนี้:

### 1. เปิด Terminal
เปิด Terminal ใน VS Code (Ctrl+J) หรือ CMD ในโฟลเดอร์โปรเจค

### 2. รันคำสั่ง
พิมพ์คำสั่งนี้แล้วกด Enter:

```bash
npx vercel --prod
```

### 3. รอ...
ระบบจะทำการ:
1.  **Build**: แปลงโค้ดเป็นเวอร์ชั่น Production
2.  **Upload**: อัพโหลดไฟล์ขึ้น Server
3.  **Deploy**: เปลี่ยนเวอร์ชั่นบนเว็บจริง

เมื่อเสร็จแล้ว Terminal จะแสดงลิ้งค์เว็บ เช่น `https://nzbill.vercel.app`

---

## วิธี Deploy (กรณีเริ่มใหม่ หรือเครื่องใหม่)

ถ้าเปลี่ยนเครื่อง หรือต้องการ Deploy โปรเจคใหม่:

### 1. Login Vercel
```bash
npx vercel login
```
*เลือก Login ด้วย GitHub (หรือ Email) แล้วไปกดยืนยันใน Browser*

### 2. เชื่อมต่อโปรเจค (Link)
```bash
npx vercel
```
ระบบจะถามคำถาม ให้ตอบตามนี้:
- **Set up and deploy?**: `y` (Yes)
- **Scope**: กด Enter (เลือก default)
- **Link to existing project?**: `n` (No - ถ้าเป็นโปรเจคใหม่) หรือ `y` (Yes - ถ้าเคยมีแล้ว)
- **Project Name**: ตั้งชื่อโปรเจค (ภาษาอังกฤษ ตัวเล็ก ห้ามมีวรรค)
- **Directory**: กด Enter (ใช้ ./ ปัจจุบัน)
- **Build settings**: กด `n` (No - ใช้ค่า Default ของ Vite ได้เลย)

### 3. Deploy Production
เมื่อ Link เสร็จแล้ว ครั้งต่อไปก็ใช้คำสั่งเดิมได้เลย:
```bash
npx vercel --prod
```

---

## คำสั่งลัดที่มีประโยชน์

- **เช็คสถานะ**: `npx vercel list` (ดูประวัติการ deploy)
- **ดู Logs**: `npx vercel logs nzbill` (ดู logs ของเว็บจริง)
